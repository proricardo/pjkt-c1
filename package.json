{
  "name": "projekt-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "build:docs": "npm run build && rm -rf docs && cp -r dist docs && touch docs/.nojekyll"
  },
  "dependencies": {
    "astro": "^4.16.18"
  }
}
